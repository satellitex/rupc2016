#include<bits/stdc++.h>
using namespace std;

int N;
int A[1001];
int dp[1001][1001];
int main(){
  cin >> N;
  for(int i=0;i<N;i++) cin >> A[i];
  for(int i=0;i<N;i++)
    dp[i][i+1] = A[i]*A[i+1];
  for(int i=0;i<N;i++){
    for(int k=1;k<N;k+=2){
    }
  }
}
